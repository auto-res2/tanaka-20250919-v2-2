
Input:
You are a cutting-edge AI researcher. Based on the instructions below, please design an experimental plan to demonstrate the effectiveness of the new research idea described in # New Methods.

# Instructions
- Propose up to three experimental strategies to verify the effectiveness of the new research idea.
- Each experiment should be realistic and take into account the experimental environment.
- For each experiment, also describe what the results would ultimately demonstrate.
- The proposed experiments must clearly highlight the effectiveness of the method.
- Consider experiments that showcase the strengths of the method from multiple perspectives.


# Experimental Environment
NVIDIA A100
VRAM：80GB
RAM：2048 GB

# Current Research Method (Target for Experiment Design)
{
    "Open Problems": "Token-level cross-entropy remains the only generally-usable objective for PEFT / LoRA instruction tuning, even though it (a) quickly over-fits the few-shot SFT sets, (b) yields over-confident, poorly calibrated probabilities, and (c) catastrophically forgets zero-shot skills learned during pre-training.  Preference-based objectives (DPO, DiscoPOP) fix alignment but make calibration even worse and still damage knowledge when β is mis-set.  A single objective that 1) preserves the base model’s knowledge, 2) exploits both labelled and preference data, 3) stays numerically stable without hand picking β, and 4) can be dropped into any HuggingFace LoRA script with <100 lines of PyTorch is still missing.",
    "Methods": "We upgrade the original CALM idea into APEX-Loss (Adaptive Pareto-Efficient miX).  Key novelties over CALM:  1. Pareto-front weight learner.  Instead of the binary switch w_c and a fixed λ, we embed the three primitive losses—Spherical (S), Preference (P), and KL (K)—into a 3-D objective space.  A tiny two-layer MLP (≈30 k parameters; frozen after meta-training) takes as input three scalars (current batch means of S, P, K) and outputs non-negative mixing weights α_s, α_p, α_k that sum to 1 through softmax.  The MLP was meta-trained off-line on synthetic mixtures of noisy-clean-preference data to approximate the hyper-volume improvement (HVI) criterion, giving a differentiable, single-pass approximation of the Pareto-optimal weight vector for each batch.  This removes hand-tuning and adapts automatically as training progresses.  2. Temperature-calibrated scoring rule.  We replace plain Spherical with a Temperature-Scaled Spherical score S_T = S(ph/τ) where τ itself is a learnable global scalar updated by gradient descent to minimise ECE on a held-out 1 % calibration split.  This gives provably strictly proper calibration while costing two extra FLOPs per token.  3. Elastic-KL with per-layer budgets.  Instead of one λ we constrain the mean KL of every transformer block’s output logits against the frozen base, enforced with the same dual-ascent trick but per layer.  This localises knowledge preservation to early layers (language syntax) while letting higher layers adapt to the new task.  4. Self-denoising mask.  Tokens whose predictive entropy is above the 80th percentile inside a batch are masked out of the gradient (no forward weight) rather than merely down-weighted; this behaves like dynamic, model-based data cleaning and adds zero extra parameters.  Full loss:  L = α_s·S_T + α_p·P + α_k·Σ_l λ_l·K_l,  computed per token (S_T, K_l) or per sequence (P), averaged with the mask.",
    "Experimental Setup": "Models: Llama-2-7B-hf, Mistral-7B-Instruct, and—new for scalability—Phi-2-2.7B.  Data:  • SFT: Alpaca-52k + 50 k additional ShareGPT dumps purposely injected with 20 % synthetic label flips for noise.  • Preference: Argilla-DPO-Mix-7k plus 5 k toxicity-preference pairs (Anthropic-HH subset) to test multi-objective behaviour.  • 1 % of SFT data held-out for on-line ECE calibration of τ.  Baselines:  (A) Cross-Entropy  (B) DPO  (C) CALM (original)  (D) APEX (ours).  Each with identical LoRA config (r=16, α=32).  Metrics:  1. MT-Bench and AlpacaEval 2.0 (GPT-4 judge).  2. Expected Calibration Error on TruthfulQA-MC and SciQ-MC.  3. Knowledge retention: zero-shot MMLU + GSM8K 5-shot.  4. Robustness: measure metric decay when adding 10 %, 30 %, 50 % synthetic label noise.  5. Multi-objective alignment: win-rate on Toxicity-avoidance eval (Anthropic-HH).  6. Efficiency: GPU hours and wall-time.  All experiments run 3 seeds on 4×A100-40 GB; statistics via paired t-test.",
    "Experimental Code": "# core APEX loss (simplified)\nclass APEXLoss(nn.Module):\n    def __init__(self, temp_init=1.0, kl_target=0.5):\n        super().__init__()\n        self.log_tau = nn.Parameter(torch.tensor(math.log(temp_init)))\n        self.meta_mlp = nn.Sequential(\n            nn.Linear(3,16), nn.SiLU(), nn.Linear(16,3))\n        self.register_buffer('lambda_l', torch.ones(32))  # for 32-layer model\n        self.kl_target = kl_target\n    def forward(self, spher, pref, kls):\n        # spher, pref: (B,)  kls: list of (B,) per layer\n        with torch.no_grad():\n            mean_vec = torch.tensor([\n                spher.mean(), pref.mean(), torch.stack(kls).mean()])\n            alpha = torch.softmax(self.meta_mlp(mean_vec),0)\n        loss = alpha[0]*spher.mean() + alpha[1]*pref.mean() + \\\n               alpha[2]*sum(l*kl.mean() for l,kl in zip(self.lambda_l,kls))\n        # dual ascent for each layer\n        with torch.no_grad():\n            for i,kl in enumerate(kls):\n                self.lambda_l[i].add_(1e-3*(kl.mean()-self.kl_target)).clamp_(0)\n        return loss\n# Temperature-scaled spherical term\nprobs = torch.softmax(logits/ self.log_tau.exp(), -1)\nS_T = 1 - (probs.gather(-1,labels[...,None]).squeeze() / probs.norm(dim=-1))\n",
    "Expected Result": "1. APEX beats DPO by +3 MT-Bench win-rate points and +4 on AlpacaEval, surpassing CALM by +1.  2. ECE reduced 45 % versus CE and 25 % versus CALM.  3. MMLU drop ≤0.3 %; GSM8K unchanged—better than all baselines.  4. Under 50 % label noise, MT-Bench score retains 88 % of clean performance (vs 60 % for CE, 75 % for CALM).  5. Toxicity avoidance win-rate +6 over DPO, showing multi-objective balancing.  6. Training cost ↑ only 6 % wall-time over CE (meta-MLP negligible).",
    "Expected Conclusion": "APEX-Loss delivers an end-to-end adaptive objective that sits on the Pareto frontier of calibration, alignment, and knowledge preservation without manual coefficient tuning.  The learned weight generator and layer-wise elastic KL let the optimiser focus capacity where it matters while freezing fundamentals, leading to higher instruction quality, markedly better probability estimates, and strong resilience to noisy or conflicting supervision.  Socially, this makes community LoRA fine-tunes safer and more stable out-of-the-box.  Academically, it unifies ideas from multi-objective optimisation, proper scoring rules, and adaptive calibration, opening a route to principled, automated loss design for future multi-modal or multi-task LLM fine-tuning."
}

---


# Reference Information from Previous Iteration

Output:
{
    "experiment_strategy": "Experiment 1 – Component Ablation & Contribution Analysis\n----------------------------------------------------------------\nGoal: Quantify how much each novel sub-module of APEX (Pareto MLP, Temp-scaled Spherical, Elastic-KL, Self-denoising mask) contributes to the four target properties: instruction quality, calibration, knowledge retention, and noise-robustness.\n\nDesign\n• Model/Data: Llama-2-7B-hf with the full training recipe already described (Alpaca 52 k + noisy ShareGPT; Argilla-DPO preferences). \n• Variants (LoRA r=16, 3 random seeds):\n  A0  Full APEX (reference)\n  A1  – Pareto MLP → replace by fixed αs=0.33,αp=0.33,αk=0.34\n  A2  – Temp-scaled Spherical → ordinary Spherical, freeze τ=1\n  A3  – Elastic-KL → single global λ tuned once on dev set\n  A4  – Self-denoising mask → conventional entropy re-weighting (no masking)\n  A5  – All four removals (degenerates to CALM-style mix)\n\nMetrics (same eval harness): MT-Bench, AlpacaEval, ECE (TruthfulQA-MC), zero-shot MMLU, GSM8K-5-shot, noise stress-test (50 % label flips), GPU hours.\n\nExpected Read-out\n• A0 should dominate A1–A5 on every axis within paired t-test α=0.05.\n• Largest MT-Bench gap is expected when removing the Pareto MLP (A1), showing adaptive weighting is critical for quality/alignment.\n• ECE should worsen most in A2, isolating the temperature-scaled scoring rule’s role.\n• Knowledge retention drops sharply in A3, confirming that per-layer KL budgets protect pretrained competence.\n• Noise stress-test sensitivity spikes in A4, validating the self-denoising mask.\n• A5 should replicate CALM numbers, proving additivity, not mere parameter lucky-shot.\nDemonstration: Clear, orthogonal gains from every component certify that the full APEX recipe—not any single trick—achieves the claimed Pareto dominance.\n\nExperiment 2 – β-Sensitivity Curve vs. DPO (Alignment–Calibration Trade-off)\n--------------------------------------------------------------------------\nGoal: Show that APEX eliminates manual β tuning by staying on or near the Pareto front across the entire alignment/calibration spectrum, whereas DPO moves along a brittle 1-D curve.\n\nDesign\n• Model: Mistral-7B-Instruct (LoRA r=16).  \n• Fixed training setup, but run DPO for β∈{0.01,0.1,0.3,1,3,10}.  Run APEX once (no hyper-parameter search).\n• For every checkpoint, measure: (1) MT-Bench, (2) ECE, (3) MMLU delta, (4) Toxicity-avoidance win-rate.\n• Plot four heat-maps: metric value vs. β; overlay horizontal APEX line.\n\nExpected Read-out\n• DPO exhibits strong metric trade-offs: high β → good alignment but disastrous ECE/knowledge; low β → opposite.  Sweet-spot narrow.\n• APEX single point lies inside the upper-right quadrant for all four plots—matching or exceeding DPO’s per-metric optimum simultaneously.\nDemonstration: The adaptive Pareto MLP makes APEX “set-and-forget”, automatically landing on a near-optimal alignment/calibration compromise that DPO can reach only by exhaustive β sweeps.\n\nExperiment 3 – Plug-and-Play Scalability & Generalisation\n---------------------------------------------------------\nGoal: Validate that APEX really is a drop-in loss usable across model sizes, domains, and hardware budgets with negligible overhead.\n\nDesign\n• Models:  \n  – Phi-2-2.7B (single A100-80 GB)  \n  – Llama-2-13B (2×A100-80 GB)  \n  – Llama-2-70B (8×A100-80 GB, ZeRO-3)\n• Domains / data:  \n  – General instruction (Alpaca+ShareGPT, 20 % noise)  \n  – Code synthesis (Code-Alpaca 20 k + 2 k preference pairs)  \n  – Medical QA (MedAlpaca 8 k + 1 k physician preference pairs)\n• For each (model, domain) pair run CE baseline and APEX with identical LoRA configs; log:   \n  – Main domain metric (e.g. HumanEval pass@1, MedQA-USMLE accuracy)  \n  – ECE on in-domain multiple-choice val set  \n  – Zero-shot MMLU (knowledge bleed)  \n  – Wall-clock hours and peak GPU memory\n• Record lines of code changed: diff of training script (should be <100).\n\nExpected Read-out\n• APEX yields consistent +2‒6 % absolute gains on domain metrics across all three model sizes while cutting ECE by ≥30 % and keeping MMLU drop <1 %.\n• Compute overhead ≤8 % in GPU hours; memory overhead negligible (<0.3 GB from MLP & buffers).\n• Code diff ≤70 lines for all experiments, visually proving simplicity.\nDemonstration: Confirms APEX scales from 2.7 B to 70 B parameters, handles specialised domains, and really is a low-friction, resource-friendly plug-in—addressing practical adoption concerns.\n\nCollective Outcome\nThese three orthogonal experiments (granular ablation, hyper-parameter-free robustness, and cross-domain scalability) will jointly demonstrate that APEX-Loss is (1) internally well-engineered, (2) externally robust without tuning, and (3) practically deployable, thereby validating the core research claims."
}
